# First Draft — Top 10 AI Companies to Invest in (H2 2026)

Static investor-committee style web deliverable for a scheduled Hermes research task.

## Contents
- `index.html` — polished static report
- `assets/generated/` — locked visual identity assets used in the deliverable
- `generate_images.py` — attempted direct OpenAI `gpt-image-2` generation script; requires `OPENAI_API_KEY`
- `create_assets.py` — local fallback SVG visual-system generator used because image API credentials were unavailable in the execution environment

## Caveat
This is a first draft. Private company metrics are not fully disclosed and should be validated with primary round documents/data-room materials before investment use.
